import React, { useState } from 'react'

const Useref = () => {
  const[num,setNum]=useState(1);

  return (
    <div>
      <h1>{num}</h1>
     <h1>(❁´◡`❁)</h1>
    </div>
  )
}

export default Useref